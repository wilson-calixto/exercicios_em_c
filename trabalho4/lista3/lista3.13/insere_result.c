#include <stdio.h>
char ***tabela_completa(int npartida, char*** resultados)
{
	int i,j;
	char ***tabela = (char ***) malloc (npartida*(sizeof(char*)));
	*tabela =  (char **) malloc (npartida*(sizeof(char*)));
	**tabela=  (char *)  malloc (10*sizeof(char)); 



	for ( i = 0; i < npartida; i += 1 ) 
	{
		 
		for ( litab = 0; litab < npartidas; litab += 1 ) 
		{
			if resultados[i][0] == 
		}

	}
}
